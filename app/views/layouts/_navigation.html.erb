<nav class="navbar navbar-default hidden-xs">
	<div class="container-fluid">
    <div class="nav navbar-nav navbar-left">
			<ul class="nav navbar-nav">
				<li>
					<%= image_tag 'logo.jpg', style: 'width:212px; height:50px; margin: 10px;'%>
        </li>
      </ul>
    </div>
    <% if user_signed_in? %>
      <%= link_to 'Home', articles_path, style: "margin-top:20px", class: "collapsed btn btn-small btn-primary pull-right" %>
    <% else %>
      <%= link_to 'Home', root_path, style: "margin-top:20px; margin-right:10px;", class: "collapsed btn btn-small btn-primary pull-right" %>
    <% end %>
  </div>
</nav>
